---
kata: 5.D.5
date: 2026-07-10
role: developer
consumes_from: Artefacts/500-dev/04-tests.md
sandbox: logsum-sandbox (local + https://github.com/v-demydov/logsum-sandbox)
deliverables: .github/workflows/ci.yml · ci-notes.md
pr: https://github.com/v-demydov/logsum-sandbox/pull/1
---

# 05 — CI Gate (GitHub Actions)

## Workflow
`.github/workflows/ci.yml` — 25 lines.
- Trigger: push + pull_request
- Runner: ubuntu-latest, Python 3.11
- Steps: checkout → setup-python → pip install ruff pytest → ruff check . → pytest -v

## Red → Green history

| Commit | Result | Cause | Diagnosis |
|--------|--------|-------|-----------|
| `163cb5e` Add CI + `import os` | RED | F401: unused `import os` in `src/logsum.py` | Implementation bug (deliberate) |
| `fea2c6c` Remove `import os` | RED | F401: unused `import pytest` in `tests/test_logsum.py` | Test bug — pytest fixtures inject without direct import |
| `47a992a` Remove `import pytest` | GREEN | — | Both checks pass |

## CI run links (green)
- https://github.com/v-demydov/logsum-sandbox/actions/runs/29088631361
- https://github.com/v-demydov/logsum-sandbox/actions/runs/29088633312

## Unexpected finding
The test file had `import pytest` as defensive boilerplate — a real F401 caught
by the same ruff gate that was meant to catch the deliberate violation.
The gate found a bug in the tests, not just the one planted in the implementation.

## Failure modes avoided

| Risk | Outcome |
|------|---------|
| Workflow in wrong folder | Placed at `.github/workflows/ci.yml` |
| Red CI fixed by deleting a test | Both fixes removed unused imports; no test was weakened or deleted |
| PR body has no evidence | PR body named the deliberate violation and expected red/green cycle |
